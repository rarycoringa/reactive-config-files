# Order application properties - Orchestration profile

spring:
    cloud:
        function:
            definition: processOrderCommand;sinkEvent
        stream:
            bindings:
                processOrderCommand-in-0:
                    destination: order_commands
                processOrderCommand-out-0:
                    destination: saga_events
                sinkEvent-in-0:
                    destination: saga_events
